spring:
  config:
    import:
      - classpath:virtualrift-observability.yml
      - classpath:virtualrift-kafka-security.yml
  application:
    name: virtualrift-reports

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/virtualrift_reports}
    username: ${DB_USER:virtualrift_reports}
    password: ${DB_PASSWORD:virtualrift}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration/reports
    baseline-on-migrate: true
    table: reports_flyway_schema_history
    user: ${DB_MIGRATION_USER:virtualrift_reports_migrator}
    password: ${DB_MIGRATION_PASSWORD:virtualrift}

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

orchestrator:
  service:
    url: ${ORCHESTRATOR_SERVICE_URL:http://localhost:8083}

tenant:
  service:
    url: ${TENANT_SERVICE_URL:http://localhost:8082}

reports:
  maintenance:
    retention-cleanup-delay-ms: ${REPORT_RETENTION_CLEANUP_DELAY_MS:3600000}

outbox:
  publisher:
    fixed-delay-ms: ${OUTBOX_PUBLISHER_DELAY_MS:1000}
    batch-size: ${OUTBOX_PUBLISHER_BATCH_SIZE:25}
    send-timeout-seconds: ${OUTBOX_SEND_TIMEOUT_SECONDS:10}

virtualrift:
  runtime:
    environment: ${VIRTUALRIFT_RUNTIME_ENVIRONMENT:local}

server:
  port: 8084

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha

management:
  server:
    port: ${MANAGEMENT_PORT:9084}
